git checkout master  # to move to the main branch
git fetch origin      # fetches all the changes
git pull origin master # pull changes from the origin remote, master branch and merge them into the local machine
git push origin master # pushes my branch
